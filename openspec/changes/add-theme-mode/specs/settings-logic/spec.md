# Settings Logic

## MODIFIED Requirements

### Requirement: Theme Selection
The Settings Dialog MUST allow selecting between "Light" and "Dark" modes.

#### Scenario: Save Theme
- Given the settings dialog is open
- When the user selects "Light"
- Then the UI updates immediately
- And the preference is saved to storage.
